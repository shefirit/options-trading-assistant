"""Research tools - the "size up any stock" side of the app.

These modules are deliberately pure: they take numbers in and give answers
out, with no network calls. The provider fetches the raw data, these turn it
into something readable, and the tests can run offline.
"""
